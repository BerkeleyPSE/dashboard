import { CareerConstants } from '../actions/career-actions';

const initialState = {};

const CareerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CareerReducer;
