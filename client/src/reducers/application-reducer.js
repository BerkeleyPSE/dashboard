import { ApplicationConstants } from '../actions/application-actions';

const initialState = {};

const ApplicationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ApplicationReducer;
