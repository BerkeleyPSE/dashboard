import { FaqConstants } from '../actions/faq-actions';

const initialState = {};

const FaqReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default FaqReducer;
